# Legacy filesystem compatibility helpers

The application HTTP server and MIDI bridge have been removed. These remaining modules support old project/backup compatibility tests and the Neon Drive build-time fixture. They are not bundled into the browser and are not a supported second application runtime. Export existing local data using the previous release before migrating; see ../../docs/setup.md.
